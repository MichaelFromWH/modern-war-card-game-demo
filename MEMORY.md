# Jarvis Memory

本文件是现代战争卡牌项目的长期记忆入口。它不替代 `GAME_MECHANICS.md`、`PLAYTEST_DEBUG_LOG.md`、飞书测试报告或 Bug 表，而是记录它们的索引、当前共识和读取顺序，让 Jarvis 在上下文压缩后可以可靠恢复项目状态。

## 使用原则

- 不确定用户偏好、产品判断、机制口径、测试结论或历史 Bug 时，先检索本文件。
- 本文件只保存长期有效的摘要、索引和下一步判断；详细事实仍写入对应源文档。
- `决策记录`、`Playtest / Debug`、`飞书报告`、`Bug 表` 都属于重要项目记忆，必须在本文件中有入口或摘要。
- 如果本文件指向飞书文档，先阅读对应飞书原文，再执行实现、测试或结论更新。
- 如果本文件、项目文档、代码表现冲突，先回到源文档和飞书标准确认口径，再改代码。

## 记忆读取顺序

1. 先读 `AGENTS.md`，确认 Jarvis 的工作方式、项目目标和安全约束。
2. 再读本文件，确认长期记忆、决策索引、飞书入口和最近状态。
3. 根据问题类型阅读源文档：
   - 机制与裁决：`GAME_MECHANICS.md`
   - 当前卡牌内容：`GAME_CONTENT_V0.5.2.md`
   - 真实对局与缺陷：`PLAYTEST_DEBUG_LOG.md`
   - 回归测试：`TEST_CASES.md`
   - 飞书资源：`docs/lark-resources.md`
4. 如果本文件或 `docs/lark-resources.md` 记录了对应飞书文档，优先打开飞书原文核对。
5. 最后再改 `src/game-data.js`、`src/main.js`、`src/online-battle-engine.js` 等实现文件。

## 用户长期偏好

- Michael 希望 Jarvis 不只是执行器，而是私人助理、产品搭档、工程协作者和成长型导师。
- Michael 偏好主动、有判断力、有产品品味的协作方式。
- 遇到机制、平衡、命名、体验不一致时，Jarvis 应主动指出并给出判断。
- 不希望长期信息只依赖上下文压缩；重要信息要进入项目文档，并通过本文件建立索引。

## 项目核心方向

- 项目是现代战争三线卡牌 WebDemo。
- 第一屏应保持可玩，不改成营销落地页。
- 核心追求是可玩性、可拓展性、易上手性和战场感。
- 新卡牌应自然接入现有标签、数值档位和反制链。
- 战术牌用于修正局面，不应成为主要杀伤来源。

## 项目源文档索引

| 记忆类型 | 源文档 | 何时读取 |
| --- | --- | --- |
| 协作身份与工作规则 | `AGENTS.md` | 每次开始重要任务前 |
| 项目入口与运行 | `README.md` | 启动、验证、理解结构时 |
| 机制真源 | `GAME_MECHANICS.md` | 涉及规则、裁决、回合体验、联机一致性时 |
| 当前内容快照 | `GAME_CONTENT_V0.5.2.md` | 查卡牌数值、技能文本、预设牌组时 |
| 旧版本内容快照 | `GAME_CONTENT_V0.5.1.md` | 对比历史规则或内容迁移时 |
| 真实对局与缺陷 | `PLAYTEST_DEBUG_LOG.md` | 排查 Bug、记录根因、验证修复时 |
| 回归测试清单 | `TEST_CASES.md` | 修改机制、卡牌或联机逻辑后 |
| 卡牌设计方法 | `card_design_method.md` | 新增或调整卡牌时 |
| 线上 1v1 路线图 | `online_1v1_roadmap.md` | 涉及联机架构、协议、房间流时 |
| 飞书资源入口 | `docs/lark-resources.md` | 需要需求、测试报告、Bug 表或飞书原文时 |

## 核心文件元数据

| 文件 | 权威范围 | 关联上游 | 更新触发 |
| --- | --- | --- | --- |
| `MEMORY.md` | 长期记忆索引、决策摘要、飞书入口、会话恢复规则 | `AGENTS.md`、飞书资源索引、重要会话结论 | 新增长期有效共识、重要决策、关键 Bug 或飞书文档入口时 |
| `AGENTS.md` | Jarvis / Codex 工作方式、项目协作规则、安全边界 | Michael 的长期偏好、项目工作流 | 协作方式、必读文件、执行规则发生变化时 |
| `docs/lark-resources.md` | 飞书项目文档、报告和表格的固定入口 | 飞书项目空间 | 新增或替换飞书文档、测试报告、Bug 表时 |
| `GAME_MECHANICS.md` | 游戏机制、裁决规则、回合体验真源 | 产品决策、测试结论、真实对局反馈 | 机制规则、卡牌效果裁决、联机一致性发生变化时 |
| `GAME_CONTENT_V0.5.2.md` | 当前版本卡牌内容快照 | `src/game-data.js` 最终运行数据 | 卡牌数值、技能文本、预设牌组或可用卡池变化时 |
| `PLAYTEST_DEBUG_LOG.md` | 真实对局反馈、缺陷根因、修复与验证记录 | Playtest、飞书 Bug 表、测试报告 | 发现、修复或验证缺陷时 |
| `TEST_CASES.md` | 本地回归测试清单 | 机制真源、飞书测试标准 | 新增机制、修复 Bug、调整上线前测试范围时 |

## 飞书记忆入口

飞书资源的本地固定索引是 `docs/lark-resources.md`。当前已记录：

- 飞书项目文件夹
- 项目资源索引文档
- V0.5.2 上线前测试用例标准
- V0.5.2 第一次上线前全面测试报告
- BUG-0001 维修满血修复回归报告
- Bug 管理多维表格

工作规则：

- 不确定测试标准或上线结论时，先查飞书测试用例标准和最近测试报告。
- 发现缺陷或上线阻塞时，先在本地 `PLAYTEST_DEBUG_LOG.md` 留痕，再按需要同步飞书 Bug 管理表。
- 如果用户提到“飞书里的记录”“之前的报告”“Bug 表”“测试标准”，优先从 `docs/lark-resources.md` 找入口。

## 决策记录索引

当前固定决策：

- `GAME_MECHANICS.md` 是机制真源。涉及规则、卡牌效果、联机裁决和回合体验时，必须同步它。
- `src/game-data.js` 是运行时卡牌与效果数据源。文档快照用于查阅，最终表现要核对运行数据。
- V0.5.2 / 20260527 的实际效果由 `src/game-data.js` 后半段覆盖层形成。
- V0.5.3 / 20260611 已接入运行规则：三类回合资源、单位禁火标记、前线反击和高空反击。源规则见 `GAME_MECHANICS.md`，验证记录见 `PLAYTEST_DEBUG_LOG.md` 的 2026-06-12 条目。
- 2026-06-12 追加修复：隐蔽前线接敌同时结算、高空接敌仅暴露、防空拦截仅敌方回合、资源 hover 说明、图鉴/组卡内部字段隐藏；线上版本 `v0.5.3-20260612-p0p1p2-1139`。
- 2026-06-12 热修：本地 AI 双部署后，“回合结束”在部署结算锁期间可排队执行；线上版本 `v0.5.3-20260612-endturn-queue-203312`，入口见 `PLAYTEST_DEBUG_LOG.md` 的“V0.5.3 双部署后回合结束按钮不可点击”。
- 当前可用卡池由 `V07_ACTIVE_CARD_IDS` 控制。
- 当前起始牌组由 `STARTER_DECKS` 控制。
- `战地维修`、`战场维修` 的 V0.5.2 确认规则：修复己方一个受损单位的全部生命；无受损单位时触发抽 2 留 1 的替代补给。

后续新增重要产品、机制或技术决策时，在这里添加一条摘要，并指向源文档、飞书链接或代码文件。

## Playtest / Debug 索引

源文档：`PLAYTEST_DEBUG_LOG.md`

当前已知重要缺陷与验证入口：

- 2026-06-12：V0.5.3 回合资源、禁火标记、前线/高空反击已完成本地 AI 与在线 1v1 smoke，入口见 `PLAYTEST_DEBUG_LOG.md`。
- 2026-06-12：P0/P1/P2 复测修复已完成本地回归和公网 WebSocket smoke，入口见 `PLAYTEST_DEBUG_LOG.md` 的“V0.5.3 P0/P1/P2 复测修复”。
- 2026-06-12：双部署后回合结束按钮不可点击热修已完成本地回归和浏览器 smoke，入口见 `PLAYTEST_DEBUG_LOG.md`。
- BUG-0001：维修满血修复回归报告，飞书入口见 `docs/lark-resources.md`。

记录规则：

- 真实对局反馈、缺陷根因、修复方案和验证结果写入 `PLAYTEST_DEBUG_LOG.md`。
- 如果某个问题影响上线判断，也要在本文件添加摘要入口。
- 若飞书 Bug 管理表已有对应记录，本文件保留 Bug 编号、简述和入口位置。

## 会话恢复规则

上下文压缩、换会话或长时间中断后，Jarvis 应按以下方式恢复：

1. 读取 `AGENTS.md` 和 `MEMORY.md`。
2. 查看 `git status --short --branch`，识别用户已有改动。
3. 根据当前任务查对应源文档和飞书入口。
4. 不凭模糊记忆下结论；关键事实必须回到本地文档、飞书文档或代码确认。
5. 完成重要工作后，更新相关源文档，并在本文件补充长期有效的索引或摘要。
