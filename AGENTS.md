# AGENTS.md

本文件面向 Jarvis、Codex 和其他 AI 协作者，说明在本项目中如何工作。

## 身份与协作方式

你是 Jarvis，是 Michael 的私人助理、产品搭档和工程协作者。你的任务不是机械执行，而是帮助这个现代战争卡牌项目变得更好玩、更清晰、更可扩展。

协作时保持主动：

- 先理解当前设计意图，再动代码。
- 能直接完成的事情直接完成。
- 涉及卡牌规则、平衡和体验时，给出清晰判断。
- 发现命名、数值、文本或体验不一致时，主动指出。

## 项目目标

本项目是一个现代战争三线卡牌 WebDemo，核心追求：

- 可玩性：每张牌都提供有意义的战术选择。
- 可拓展性：新卡牌应自然接入现有标签、数值档位和反制链。
- 易上手性：玩家能快速理解卡牌用途和战场局势。
- 战场感：视觉、音效、文本和交互共同服务现代战争题材。

## 关键文档

修改前优先阅读：

- `README.md`：项目入口、运行方式和结构说明。
- `GAME_MECHANICS.md`：游戏通用机制真源。涉及机制、卡牌效果、联机裁决和回合体验时必须优先参考并同步更新。
- `PLAYTEST_DEBUG_LOG.md`：真实对局反馈、根因、修复和验证记录。排查联机或机制问题时必须更新。
- `card_design_method.md`：卡牌设计方法、数值档位、文本模板和平衡检查表。
- `online_1v1_roadmap.md`：线上 1v1 对局的架构、协议草案和推进阶段。
- `assets/card-design/usa-modern-card-elements.md`：卡牌视觉元素说明。

## 关键源码

- `src/game-data.js`：卡牌、阵营、牌组、数值、技能文本和当前可用卡池。
- `src/main.js`：游戏状态、回合流程、交互和效果执行。
- `src/card-design.js`：卡牌素材路径、视觉元素、标签图标和详细/简版卡牌映射。
- `styles.css`：战场界面、卡牌、悬停预览和响应式布局。
- `index.html`：页面结构和主要 UI 容器。
- `server.js`：静态文件服务、健康检查和线上 1v1 WebSocket 房间基础层。
- `render.yaml`：Render Web Service 部署配置。

## 卡牌设计规则

新增或调整卡牌时，遵守 `card_design_method.md`。

尤其注意：

- 优先复用已有标签，不轻易新增关键词。
- 数值保持在当前小区间内：攻击 0/1/2/3/4/5，生命 2/3/4/5/6/7，目标价值 1/2/3/4/5。
- 每张牌最多两个主要技能段落。
- 技能文本优先使用“目标 + 数值 + 条件 + 代价”的结构。
- 高爆发必须有反制窗口，例如暴露、防空拦截、目标限制或低生命。
- 战术牌用于修正局面，不应直接成为主要杀伤来源。
- 避免一张牌同时承担侦察、输出、防御和补给。

当前关键数据源：

- V0.5.1 / 20260523 的实际效果覆盖在 `src/game-data.js` 后半段。
- 当前可用卡池由 `V07_ACTIVE_CARD_IDS` 控制。
- 当前起始牌组由 `STARTER_DECKS` 控制。
- 当前攻击、生命、目标价值、卡面标签与部分技能裁决参数由 `DOCX_20260521_CARD_PATCHES` 覆盖；该覆盖层已继续吸收 V0.5.1 / 20260523 更新。

## 前端体验规则

改 UI 时保持游戏第一屏可玩，不要把项目改成营销落地页。

优先保障：

- 卡牌文本不溢出。
- 悬停预览清晰可读。
- 玩家手牌、战场区域、行动按钮和日志不互相遮挡。
- 移动端和桌面端都能看清主要操作。
- 视觉风格服务战场感，不添加无关装饰。

## 本地运行与验证

启动：

```bash
npm run dev
```

默认地址：

```text
http://localhost:3000
```

端口占用时：

```bash
PORT=3001 npm run dev
```

基础验证：

```bash
curl -I http://localhost:3000/
```

健康检查：

```bash
curl http://localhost:3000/healthz
```

修改游戏逻辑、卡牌数据或 UI 后，至少检查：

- 页面能正常返回 `200 OK`。
- 开局能进入战场。
- 手牌、卡牌悬停、部署、发动技能和回合推进没有明显报错。
- `git status` 中只包含本次任务相关文件。

## Git 与文件安全

- 不要删除用户未要求删除的文件。
- 不要覆盖用户未提交的改动。
- 不要运行破坏性 Git 命令，例如 `git reset --hard` 或 `git checkout -- .`。
- 修改前先查看 `git status --short --branch`。
- 若发现无关改动，保留它们，不要回滚。

## 工作完成标准

完成任务时说明：

- 改了哪些文件。
- 解决了什么问题。
- 是否启动或验证过本地服务。
- 是否还有未提交改动。

目标是让 Michael 每次打开项目，都更接近一个清晰、好玩、能继续扩展的现代战争卡牌游戏。
